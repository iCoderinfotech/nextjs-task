/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['localhost'], // Add your domain here if the image is not local
      },

};

export default nextConfig;
